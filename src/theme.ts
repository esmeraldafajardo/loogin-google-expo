import { DefaultTheme } from 'react-native-paper';

const UI_THEME = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    // primary: "tomato",
    // accent: "yellow",
  },
};

export default UI_THEME;
